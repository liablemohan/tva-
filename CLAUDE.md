# Project notes

- `CHECKPOINT.md` (and its visual graph `Chat Checkpoint.dc.html`) is the canonical record of this project's history and decisions. Read it before answering questions about past work or making changes, and update it when a significant decision or direction changes.
- The design lives in `index.html`; all hero/hover animation logic in `hero-animation-controller.js`.
